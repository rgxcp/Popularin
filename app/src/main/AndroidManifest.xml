<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.fairportstudios.popularin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.AddReviewActivity" />
        <activity android:name=".activities.CreditDetailActivity" />
        <activity android:name=".activities.DiscoverFilmActivity" />
        <activity android:name=".activities.EditProfileActivity" />
        <activity android:name=".activities.EditReviewActivity" />
        <activity android:name=".activities.EmptyAccountActivity" />
        <activity android:name=".activities.FavoritedByActivity" />
        <activity android:name=".activities.FilmDetailActivity" />
        <activity android:name=".activities.FilmReviewActivity" />
        <activity android:name=".activities.LikedByActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.ReviewActivity" />
        <activity android:name=".activities.SignInActivity" />
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.SocialActivity" />
        <activity android:name=".activities.UserDetailActivity" />
        <activity android:name=".activities.UserFavoriteActivity" />
        <activity android:name=".activities.UserReviewActivity" />
        <activity android:name=".activities.UserWatchlistActivity" />
        <activity android:name=".activities.WatchlistedByActivity" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>